import React from "react";
import { Typography } from "@mui/material";

const Platform = () => {
  return (
    <>
      <Typography>Platform Page</Typography>
      <Typography>Platform Page</Typography>
      <Typography>Platform Page</Typography>
      <Typography>Platform Page</Typography>
    </>
  );
};

export default Platform;
